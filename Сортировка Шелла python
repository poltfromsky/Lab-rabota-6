# Сортировка Шелла (Shell Sort)
# Алгоритм улучшает сортировку вставками, сравнивая элементы через промежутки (gap),
# постепенно уменьшая этот промежуток до 1.

def shell_sort(arr):
    n = len(arr)
    gap = n // 2  # Начальный шаг — половина длины массива

    # Уменьшаем gap вдвое на каждом шаге
    while gap > 0:
        # Сортируем элементы, отстоящие друг от друга на gap
        for i in range(gap, n):
            temp = arr[i]
            j = i
            # Сдвигаем элементы, пока не найдем правильное место для temp
            while j >= gap and arr[j - gap] > temp:
                arr[j] = arr[j - gap]
                j -= gap
            arr[j] = temp
        gap //= 2  # уменьшаем шаг

# Пример использования
if __name__ == "__main__":
    arr = [12, 34, 54, 2, 3]
    print("Исходный массив:", arr)

    shell_sort(arr)

    print("Отсортированный массив:", arr)
