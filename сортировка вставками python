# Сортировка вставками (Insertion Sort)
# Алгоритм проходит по списку и вставляет каждый элемент в правильное место
# в уже отсортированной части массива.

def insertion_sort(arr):
    # Начинаем со второго элемента, т.к. первый уже считается отсортированным
    for i in range(1, len(arr)):
        key = arr[i]       # элемент, который нужно вставить
        j = i - 1          # индекс предыдущего элемента

        # Сдвигаем элементы массива, которые больше key, на одну позицию вправо
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1

        # Вставляем key на найденное место
        arr[j + 1] = key

# Пример использования
if __name__ == "__main__":
    array = [12, 11, 13, 5, 6]
    print("Исходный массив:", array)

    insertion_sort(array)

    print("Отсортированный массив:", array)
