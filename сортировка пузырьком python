# Сортировка пузырьком (Bubble Sort)
# Алгоритм проходит по списку, сравнивает соседние элементы и меняет их местами,
# если они стоят в неправильном порядке.

def bubble_sort(arr):
    n = len(arr)
    # Внешний цикл контролирует количество проходов по массиву
    for i in range(n - 1):
        # Внутренний цикл выполняет сравнение соседних элементов
        for j in range(n - i - 1):
            if arr[j] > arr[j + 1]:
                # Меняем местами элементы, если порядок неправильный
                arr[j], arr[j + 1] = arr[j + 1], arr[j]

# Пример использования
if __name__ == "__main__":
    arr = [64, 34, 25, 12, 22, 11, 90]
    print("Исходный массив:", arr)

    bubble_sort(arr)

    print("Отсортированный массив:", arr)
